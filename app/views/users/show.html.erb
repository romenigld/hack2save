<div class="row">
  <div class="col-md-6">
    <h1><%= @user.profile.name %> <%= @user.profile.last_name %></h1>
    <h3></span> <%= @user.profile.job_title %></h3>
    <div class="well profile-block profile-description">
      <h3>Description</h3>
      <%= @user.profile.description %>
    </div>
      <div class="well profile-block contact-info">
        <h3>Contact Info</h3>
        <%= @user.profile.phone_number %><br/>
        <%= @user.profile.contact_email %><br/>
      </div>
      <h4>Avatar vários tamanhos</h4>
      Thumb:
      <%= image_tag(@user.profile.avatar.thumb) if @user.profile.avatar %>
      Medium:
      <%= image_tag(@user.profile.avatar.medium) if @user.profile.avatar %>
      Big:
      <%= image_tag(@user.profile.avatar.big) if @user.profile.avatar %>
      <hr />
  </div>
</div>
